/**
 * Package contains <code>GithubModule</code> class which contains configuration for using either real Github API or mock Github API data
 * 
 * <p>
    This package contains <code>GithubModule</code> class which depending on the configuration, uses real Github API or mock GIthub API data.
    If the app is being used in production, it injects real Github API. Or if the app is being run in test mode, mock Github API data is being stored.    
 * 
 * @author Mrinal Rai
 * 
 */
package modules;
