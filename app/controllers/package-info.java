/**
 * Package containing all the controllers required for the group and individual tasks
 * 
 * <p>
    This package contains a controller class, <code>HomeController</code> which  extends Controller and implements WSBodyReadables.
    <p>
    It contains that return a result (HTTP response) and handle all the events. Based on the http response from the Github API,
    each corresponding action updates the model and which in turn, subsequently updated the view.   
    
 * 
 * @author Mrinal Rai
 * 
 */
package controllers;
